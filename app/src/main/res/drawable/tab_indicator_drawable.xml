<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Bottom line to act as the tab indicator -->
    <item
        android:bottom="-2dp"
        android:left="-2dp"
        android:right="-2dp">
        <shape android:shape="rectangle">
            <solid android:color="@color/test_color_1" />
            <corners android:radius="@dimen/dimen_10" /> <!-- Set radius to control the corner curvature -->
        </shape>
    </item>

</layer-list>
