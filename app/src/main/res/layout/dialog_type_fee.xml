<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/download_receipt_bg"
    android:padding="15dp">

    <EditText
        android:id="@+id/fee_input"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:layout_marginTop="16dp"
        android:autofillHints="name"
        android:background="@drawable/edit_text_bg"
        android:ems="10"
        android:fontFamily="@font/montserrat"
        android:gravity="start|top"
        android:hint="Fee name"
        android:inputType="textCapSentences"
        android:minHeight="48dp"
        android:paddingStart="10dp"
        android:paddingTop="10dp"
        android:paddingEnd="5dp"
        android:paddingBottom="10dp"
        android:textSize="14sp" />

    <androidx.appcompat.widget.SwitchCompat
        android:id="@+id/mandatory_btn"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_below="@id/fee_input"
        android:layout_marginTop="16dp"
        android:textAlignment="textEnd" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/fee_input"
        android:layout_alignBaseline="@id/mandatory_btn"
        android:layout_marginStart="10dp"
        android:layout_toEndOf="@id/mandatory_btn"
        android:fontFamily="@font/montserrat"
        android:text="Mandatory"
        android:textColor="@color/text_bg_color"
        android:textSize="14sp" />

    <androidx.cardview.widget.CardView
        android:id="@+id/fee_btn"
        android:layout_width="0dp"
        android:layout_height="50dp"
        android:layout_below="@id/mandatory_btn"
        android:layout_alignParentStart="true"
        android:layout_alignParentEnd="true"
        android:layout_marginTop="32dp"
        android:layout_marginBottom="20dp"
        android:backgroundTint="@color/color_10"
        android:clickable="true"
        android:clipToPadding="false"
        android:focusable="true"
        android:foreground="?android:attr/selectableItemBackground"
        app:cardCornerRadius="10dp"
        app:cardElevation="5dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:fontFamily="@font/montserrat"
            android:text="Done"
            android:textColor="@color/white"
            android:textSize="16sp" />

    </androidx.cardview.widget.CardView>


</RelativeLayout>